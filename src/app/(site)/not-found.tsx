import { NotFoundBody } from '@/components/NotFoundBody';

/** notFound() from a public page. The (site) layout supplies the chrome. */
export default function NotFound() {
  return <NotFoundBody />;
}
